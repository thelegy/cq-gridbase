[project]
name = "cq-gridbase"
dependencies = [
  "apply_to_each_face"
  "cadquery",
  "cqwild",
]
dynamic = ["version", "readme"]

[tool.setuptools.packages.find]
where = ["src"]
namespaces = false

[tool.setuptools.dynamic]
readme = {file = ["README.md"]}
